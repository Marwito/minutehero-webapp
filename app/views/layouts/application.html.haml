!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Www'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Www'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track' => 'reload'
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      .row
        .col-md-12= breadcrumbs
      = render 'layouts/messages'
      = yield
    %footer
      .container
        %p
          = link_to 'Technology', page_path('technology')
          |
          = link_to 'About Us', page_path('about')
          |
          = link_to 'MinuteHero Website', 'http://www.minutehero.net'
